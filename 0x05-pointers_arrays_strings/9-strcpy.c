#include "main.h"

/**
 * _strcpy - copies string
 * @dest: string pointer
 * @src: string pointer
 * Return: pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	int len;
	int i;

	for (len = 0; len != '\0'; len++)
	{

	}

	for (i = 0; i <= src[len]; i++)
		dest[i] = src[i];

	return (dest);
}
