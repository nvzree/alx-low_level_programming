#include "main.h"

/**
 *int _abs - computes absolute value of an integer
 *@n: integer value
 *Returns: absolute value
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
